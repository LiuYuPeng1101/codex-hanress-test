spring:
  application:
    name: order-mcp-adapter
  ai:
    mcp:
      server:
        protocol: STREAMABLE
        name: order-mcp-server
        version: 1.0.0
        type: SYNC
        streamable-http:
          mcp-endpoint: /mcp

order:
  backend:
    base-url: ${ORDER_BACKEND_BASE_URL}
