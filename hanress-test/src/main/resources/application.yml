spring:
  application:
    name: hanress-test
  ai:
    mcp:
      server:
        protocol: STREAMABLE
        name: order-mcp-server
        version: 1.0.0
        type: SYNC

        streamable-http:
          mcp-endpoint: /mcp
agent:
  order:
    workspace: D:/daima/codex-agent/hanress-test